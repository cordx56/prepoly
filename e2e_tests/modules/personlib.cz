import std.collections.{ HashMap }

type Person = {
    name: string
}

fun Person.display(self) {
    println(self.name)
}

fun map() {
    let map = HashMap.new()
    map.set(0, Person { name: "person" })
    return map
}
