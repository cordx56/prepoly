//! Structural record relations, re-exported from the single authority
//! (`brass_typesys::structural`) so existing `crate::structural::` paths
//! keep working while every pass shares one implementation.

pub use brass_typesys::structural::*;
